# src/apps/daemon/cmd

## Purpose
Cobra command composition roots. Commands wire dependencies and daemon lifetime; domain behavior belongs in internal modules.

## Contents
- Direct files (20): `daemon_client.go`, `daemon_client_test.go`, `diagnose.go`, `doctor.go`, `host_network_watchdog.go`, `host_network_watchdog_test.go`, `hotkey_stub.go`, `hotkey_windows.go`, `proxy.go`, `root.go`, `sandbox_linux.go`, `sandbox_other.go` …
- Child folders: `watchdog/`.

## Interfaces and dependencies
- Go package: `cmd`.
- Notable exported surface: `Execute`, `Version`, `WebDist`.
- Daemon-local dependencies: `internal/adapters/api`, `internal/analysis/diagnostics`, `internal/analysis/provider`, `internal/foundation/capabilities`, `internal/foundation/config`, `internal/foundation/crypto`, `internal/foundation/secrets`, `internal/foundation/store`, `internal/integrations/notifier`, `internal/integrations/sub`, `internal/native/bridge`, `internal/networking/dns`, `internal/networking/proxyconfig`, `internal/platform/system`, `internal/runtime/decoy`, `internal/runtime/proxy`, `internal/runtime/runtimecore`, `internal/workflows/jobs`, `internal/workflows/scheduler`.
- Direct daemon callers: none detected.
- External/shared imports: 2 distinct package(s); inspect source before changing dependency contracts.

## Invariants
- Keep behavior local to the named module; move cross-cutting policy to its canonical owner instead of duplicating it here.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
