# src/apps/daemon/internal/protocols/reliable

## Purpose
Reliable transport helpers used by DNS/networking implementations.

## Architectural role
- Daemon dependency band: `protocols` (rank 1; lower rank = deeper implementation).
- Cross-band imports may point only to a lower rank. Same-band imports are allowed when they remain within one architectural layer.

## Contents
- Direct files (2): `reliable.go`, `reliable_test.go`

## Interfaces and dependencies
- Go package: `reliable`.
- Notable exported surface: `Command`, `Config`, `DNSProfile`, `Delivery`, `DirectProfile`, `Frame`, `Kind`, `New`, `Op`, `Result` …
- Daemon-local dependencies: none.
- Direct daemon callers: `internal/networking/dns`, `internal/protocols/arq`.

## Invariants
- `protocols` code must not import a higher-rank daemon band.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
