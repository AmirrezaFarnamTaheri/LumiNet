# src/apps/daemon/internal/networking/routing/domain_data

## Purpose
Domain, per-app, and rule-based routing policy/data ownership, including embedded domain lists and dynamic override matching.

## Architectural role
- Daemon dependency band: `networking` (rank 2; lower rank = deeper implementation).
- Cross-band imports may point only to a lower rank. Same-band imports are allowed when they remain within one architectural layer.

## Contents
- Direct files (13): `category-ads-all.txt`, `category-ads.txt`, `category-anticensorship.txt`, `category-doh.txt`, `category-media.txt`, `category-service-access.txt`, `category-vpnservices.txt`, `doh-domains-overall.txt`, `doh-ipv4.txt`, `doh-ipv6.txt`, `geolocation-!cn.txt`, `geolocation-cn.txt` …

## Interfaces and dependencies
- See direct files and parent context for the interface consumed by sibling/parent modules.

## Invariants
- `networking` code must not import a higher-rank daemon band.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
