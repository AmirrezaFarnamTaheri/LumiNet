# Generated mobile binding

`luminet.aar` is generated from `src/apps/daemon/internal/adapters/mobilebind` and is intentionally ignored by Git. CI/release and local mobile build scripts must place the generated AAR in this directory before invoking the Android app build.
