# src/packages/lumicore/src/ffi

## Purpose
C ABI envelopes, exports, versioning, streaming ownership, and host-call dispatch.

## Contents
- Direct files (13): `async_exports.rs`, `binary_bridge.rs`, `binary_scan.rs`, `callbacks.rs`, `envelope.rs`, `exports.rs`, `ios_ffi.rs`, `json_bridge.rs`, `memory_bridge.rs`, `mod.rs`, `panic_guard.rs`, `streaming.rs` …

## Interfaces and dependencies
- Rust module membership is controlled by `mod.rs`/`lib.rs`; Cargo is compile authority.
- Host-facing changes must preserve the versioned FFI envelope and private Go ABI declaration checks.

## Invariants
- Do not advertise or promote dormant native surfaces without the Cargo/ABI/CGO verification gate.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
