# LumiCore mobile alternates

Obsolete Rust Android/JNI and tun2socks peers preserved after the Android product converged on the generated Go mobile binding and userspace Go TUN adapter.

The files are kept directly in this directory because each has a unique name and the adjacent `ALTERNATES.csv` records its role and origin. They are not part of Cargo's live module graph.
