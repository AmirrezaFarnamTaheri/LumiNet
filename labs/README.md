# LumiNet Labs

`labs/` is the single non-authoritative home for preserved experiments, porting fragments, alternate implementations, and historical capability evidence.

Nothing under `labs/` is part of a supported product source graph. Promotion requires a named production owner, explicit invariants, dependency/security review, focused executable evidence, and removal of any competing live authority. Preserved files remain hash-accounted where catalogs are present.
