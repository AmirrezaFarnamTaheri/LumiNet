# Android runtime alternates

These sources are preserved byte-for-byte from pre-unification Android runtime peers. They are reference/experimental implementations, not compiled application sources. `ALTERNATES.csv` binds each original path to its preserved path, size, and SHA-256.
