// Ported from: okhttp-master
// Target path: desktop/android/okhttp_doh_client.kt

package com.luminet.android

class OkHttpDoHClient {
    fun fetch() {
        println("okhttp_doh_client: Porting okhttp-dnsoverhttps client wrapper, raw DNS query wire-format codec, and bootstrap resolver");
    }
}
