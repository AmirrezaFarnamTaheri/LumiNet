# Desktop platform alternates

These files were previously stored under the supported Wails desktop product root but have no import or build edge from that host. They are preserved byte-for-byte as platform/prototype evidence rather than presented as shipped desktop runtime.

`ALTERNATES.csv` records original path, preserved path, byte count, and SHA-256.
