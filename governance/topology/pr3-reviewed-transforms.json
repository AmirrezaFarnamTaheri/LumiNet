{
  "schema": "v1",
  "scope": "PR #3 audit remediation review attestations for baseline files whose relocated content intentionally differs from the original repository baseline",
  "allowed_transforms": {
    "conductor/product-guidelines.md": "Reviewed relocation: governance wording was updated during the audit remediation while preserving this document as the authoritative product-guideline surface.",
    "core/src/security/memory.rs": "Reviewed security remediation: the relocated LumiCore memory-security implementation intentionally differs from baseline due to memory-safety and secret-handling fixes covered by PR #3.",
    "desktop/frontend/vite.config.ts": "Reviewed build-tooling remediation: the relocated control-ui Vite configuration intentionally differs from baseline to match the consolidated frontend topology and current build contract.",
    "server/internal/api/ipc_listener_test.go": "Reviewed API test remediation: the relocated IPC listener tests intentionally differ from baseline to exercise the hardened listener/security behavior in the current daemon topology.",
    "server/internal/api/middleware_test.go": "Reviewed API test remediation: the relocated middleware tests intentionally differ from baseline to match hardened request and middleware behavior.",
    "server/internal/api/handlers_telegram_test.go": "Reviewed API test remediation: the relocated Telegram handler regression test intentionally differs from baseline to assert the hardened SSRF-safe fetching boundary without weakening production URL validation.",
    "server/internal/bridge/shm_posix.go": "Reviewed native-boundary remediation: the relocated POSIX shared-memory bridge intentionally differs from baseline due to lifecycle and native-boundary correctness fixes.",
    "server/internal/proxy/pixel_stego.go": "Reviewed proxy remediation: the relocated pixel-steganography implementation intentionally differs from baseline due to correctness and safety fixes in the runtime proxy subsystem.",
    "server/internal/proxy/stego_test.go": "Reviewed proxy test remediation: the relocated steganography tests intentionally differ from baseline to cover the current hardened implementation.",
    "server/internal/scanner/cloudflare_deployer_test.go": "Reviewed scanner test remediation: the relocated Cloudflare deployer tests intentionally differ from baseline to match current deployment validation behavior.",
    "server/internal/scanner/syn_scanner.go": "Reviewed scanner remediation: the relocated SYN scanner intentionally differs from baseline due to scanner correctness, lifecycle, and networking fixes.",
    "server/internal/scanner/syn_scanner_test.go": "Reviewed scanner test remediation: the relocated SYN scanner tests intentionally differ from baseline to cover the remediated scanner behavior.",
    "server/internal/store/db.go": "Reviewed persistence remediation: the relocated database implementation intentionally differs from baseline due to storage correctness and failure-semantics fixes.",
    "server/internal/store/node_store.go": "Reviewed persistence remediation: the relocated node store intentionally differs from baseline due to storage correctness and transactional behavior fixes.",
    "server/internal/system/wfp_dynamic_session.c": "Reviewed Windows platform remediation: the relocated WFP dynamic-session implementation intentionally differs from baseline due to privileged networking lifecycle fixes.",
    "server/internal/system/wfp_leak_prevention.c": "Reviewed Windows platform remediation: the relocated WFP leak-prevention implementation intentionally differs from baseline due to fail-safe networking and cleanup fixes.",
    "server/internal/webui/dist/favicon.svg": "Reviewed generated UI artifact change: the relocated favicon intentionally differs from baseline as a regenerated control-ui distribution artifact corresponding to the remediated frontend.",
    "server/internal/webui/dist/icons.svg": "Reviewed generated UI artifact change: the relocated icon sprite intentionally differs from baseline as a regenerated control-ui distribution artifact corresponding to the remediated frontend."
  }
}
